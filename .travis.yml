language: swift
osx_image: xcode10.2
branches:
  only:
    - master
before_install:
  - gem install fastlane --no-rdoc --no-ri --no-document --quiet
install: true
script:
  - fastlane scan --scheme "TAU" --device "iPhone XR" --clean
